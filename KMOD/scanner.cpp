#include "scanner.h"

NTSTATUS
KmScanIntSigned8(
  PVOID base,
  SIZE_T size,
  BYTE value)
{
  NTSTATUS status = STATUS_SUCCESS;
  return status;
}

NTSTATUS
KmScanIntUnsigned8(
  PVOID base,
  SIZE_T size,
  BYTE value)
{
  NTSTATUS status = STATUS_SUCCESS;
  return status;
}

NTSTATUS
KmScanIntSigned16(
  PVOID base,
  SIZE_T size,
  PBYTE value)
{
  NTSTATUS status = STATUS_SUCCESS;
  return status;
}

NTSTATUS
KmScanIntUnsigned16(
  PVOID base,
  SIZE_T size,
  PBYTE value)
{
  NTSTATUS status = STATUS_SUCCESS;
  return status;
}

NTSTATUS
KmScanIntSigned32(
  PVOID base,
  SIZE_T size,
  PBYTE value)
{
  NTSTATUS status = STATUS_SUCCESS;
  return status;
}

NTSTATUS
KmScanIntUnsigned32(
  PVOID base,
  SIZE_T size,
  PBYTE value)
{
  NTSTATUS status = STATUS_SUCCESS;
  return status;
}

NTSTATUS
KmScanReal32(
  PVOID base,
  SIZE_T size,
  PBYTE value)
{
  NTSTATUS status = STATUS_SUCCESS;
  return status;
}

NTSTATUS
KmScanReal64(
  PVOID base,
  SIZE_T size,
  PBYTE value)
{
  NTSTATUS status = STATUS_SUCCESS;
  return status;
}

NTSTATUS
KmScanBytes(
  PVOID base,
  SIZE_T size,
  PBYTE bytes,
  ULONG byteCount)
{
  NTSTATUS status = STATUS_SUCCESS;
  return status;
}